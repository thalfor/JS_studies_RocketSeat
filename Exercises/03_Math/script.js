//
let numberOne = Number(prompt("qual o primeiro número?"));
let numberTwo = Number(prompt("qual o segundo número?"));
const soma = numberOne + numberTwo;
const subtract = numberOne - numberTwo;
const multiplic = numberOne * numberTwo;
const division = numberOne / numberTwo;
const resto = numberOne % numberTwo;
alert(
  `Soma: ${soma}. Subtração: ${subtract}. Multiplicação: ${multiplic}. Divisão: ${division}. Resto: ${resto}`
);
//
